@model IEnumerable<MusicHelp.Models.InstrumentListItem>

@{
    ViewBag.Title = "Instrument Index";
}

<h2>Here's our current list of supported instruments...</h2>

<div class="instrument row">
    @foreach (var item in Model)
    {
        <div class="col-md-6 one-container">
            <div class="one-card">
                <div class="one-header text-center">
                    @Html.ActionLink(item.InstrumentName, "Details", new { id = item.InstrumentID })
                </div>
                <div class="one-body text-center">
                    @Html.ActionLink("Update", "Edit", new { id = item.InstrumentID }) •
                    @Html.ActionLink("Remove", "Delete", new { id = item.InstrumentID })
                </div>
            </div>
        </div>
    }
</div>

<div class="instrument-footer row bottom text-center">
    <p>
        Don't see what you're looking for? @Html.ActionLink("Add New Instrument!", "Create")
    </p>
</div>

<!--Starred JavaScript in case starring method wants to get added to Instruments at later time.-->
@section scripts {
    <script language="javascript" type="text/javascript">
        $(document).ready(function () {
            $("i[data-toggle='star']")
                .css({ "cursor": "pointer" })
                .click(function (e) {
                    var target = $(e.target);
                    var noteId = target.attr("data-id");
                    var starSet = target.hasClass("glyphicon-star");

                    $.ajax({
                        method: starSet ? "DELETE" : "PUT",
                        url: "/api/note/" + noteId + "/star",
                        data: null
                    })
                        .done(function (r) {
                            target.removeClass();
                            target.addClass("glyphicon " + (starSet ? "glyphicon-star-empty" : "glyphicon-star"));
                        })
                        .fail(function (r) {
                            alert("Failed to change star status");
                        });
                });
        });
    </script>
}
